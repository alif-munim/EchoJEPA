# configs/train/vitb16/cooldown-21-mimic-224px-16f.yaml
# V-JEPA 2.1 cooldown on MIMIC-IV-Echo, ViT-B with LR decay (same 16 frames as pretrain).
# Decays LR from 1.75e-4 → 1e-6 over 30 epochs to stabilize features.
# Uses pretrain checkpoint e169.pt (pre-collapse, 124 data passes).
#
# Launch:
#   python -m app.main --fname configs/train/vitb16/cooldown-21-mimic-224px-16f.yaml --devices cuda:0 cuda:1 cuda:2 cuda:3 cuda:4 cuda:5 cuda:6 cuda:7
app: vjepa_2_1
nodes: 1
tasks_per_node: 8
cpus_per_task: 16
mem_per_gpu: 220G
folder: /home/sagemaker-user/user-default-efs/vjepa2/checkpoints/cooldown_21/mimic/vjepa2_1_vitb_224px_16f

data:
  dataset_type: VideoDataset
  datasets:
    - /home/sagemaker-user/user-default-efs/vjepa2/data/csv/mimic_annotations_s3.csv
  datasets_weights: [1.0]
  batch_size: 128               # same as pretrain (16 frames)
  crop_size: 224
  patch_size: 16
  dataset_fpcs: [16]            # same as pretrain
  fps: 8                        # same as pretrain
  tubelet_size: 2
  num_workers: 4                # 8 exhausts /dev/shm on crash-restart cycles
  persistent_workers: true
  pin_mem: true

data_aug:
  auto_augment: false
  motion_shift: false
  random_resize_aspect_ratio: [0.9, 1.1]
  random_resize_scale: [0.5, 1.0]
  reprob: 0.0

loss:
  loss_exp: 1.0
  shift_by_n: 0
  predict_all: true
  weight_distance_loss: false
  offset_context_loss: false

mask:
  - aspect_ratio: [0.75, 1.5]
    full_complement: false
    max_keep: null
    max_temporal_keep: 1.0
    num_blocks: 8
    spatial_scale: [0.15, 0.15]
    temporal_scale: [1.0, 1.0]
  - aspect_ratio: [0.75, 1.5]
    full_complement: false
    max_keep: null
    max_temporal_keep: 1.0
    num_blocks: 2
    spatial_scale: [0.7, 0.7]
    temporal_scale: [1.0, 1.0]

meta:
  dtype: bfloat16
  eval_freq: 100
  load_checkpoint: true
  read_checkpoint: null
  save_every_freq: 5
  s3_checkpoint_uri: "s3://sagemaker-hyperpod-lifecycle-495467399120-usw2/vjepa2-artifacts/checkpoints/vjepa-2.1-b-cooldown/"
  seed: 239
  use_sdpa: true

model:
  model_name: vit_base
  pred_depth: 12
  pred_embed_dim: 384
  pred_num_heads: 12
  n_output_distillation: 1
  lambda_value_vid: 0.5
  lambda_value_img: 0.5
  lambda_progressive: false
  levels_predictor: 1
  normalize_predictor: false
  modality_embedding: true
  img_temporal_dim_size: 1
  interpolate_rope: true
  uniform_power: true
  use_activation_checkpointing: true
  use_mask_tokens: true
  use_rope: true
  zero_init_mask_tokens: true
  has_cls_first: false
  is_causal: false
  pred_is_causal: false
  n_registers: 0
  n_registers_predictor: 0

optimization:
  is_anneal: true
  resume_anneal: true
  force_load_pretrain: true
  anneal_ckpt: /home/sagemaker-user/user-default-efs/vjepa2/checkpoints/pretrain_21/mimic/vjepa2_1_vitb_224px_16f/e169.pt

  ema: [0.99925, 0.99925]
  weight_decay: 0.04
  final_weight_decay: 0.04

  # LR: linear decay from pretrain peak -> 1e-6
  start_lr: 1.75e-4
  lr: 1.75e-4
  final_lr: 1.0e-6

  ipe: 300
  ipe_scale: 1.0

  warmup: 0

  # 60 epochs * 300 ipe = 18K steps (matches V-JEPA 2.0 ViT-L cooldown)
  epochs: 60

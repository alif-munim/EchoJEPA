"""EchoSet-JEPA offline data preparation scripts.

Modules
-------
- ``element_grouping``   — ``(view_family, modality, phase_bucket)`` grouping
- ``sample_K``            — fixed-K view-stratified sampler
- ``target_difficulty_audit`` — pre-pretrain audit (§10 of the plan)
"""
